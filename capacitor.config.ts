import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.orthocompanion.app",
  appName: "Ortho Companion",
  webDir: "dist",
};

export default config;
